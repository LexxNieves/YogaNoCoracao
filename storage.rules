rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Permite acesso para usuários autenticados
      allow read, write: if request.auth != null;
      
      // Para desenvolvimento, pode ser mais permissivo:
      // allow read, write: if true;
    }
  }
}
